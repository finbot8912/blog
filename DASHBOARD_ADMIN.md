# 🔐 대시보드 관리자 가이드

## 관리자 로그인

### 접근 방법
1. 메인 애플리케이션 헤더에서 📊 아이콘 클릭
2. 또는 직접 `/dashboard.html` 접속
3. 관리자 로그인 화면이 표시됩니다

### 관리자 계정 정보

```
ID: admin
비밀번호: myst2204@@
```

⚠️ **보안 주의사항**: 
- 이 정보는 절대 외부에 공유하지 마세요
- 정기적으로 비밀번호를 변경하세요
- 프로덕션 환경에서는 반드시 비밀번호를 변경하세요

## 비밀번호 변경 방법

### 1. 코드에서 직접 변경 (권장)

`public/dashboard.html` 파일에서:

```javascript
// 관리자 인증 정보
const ADMIN_CREDENTIALS = {
    id: 'admin',
    password: 'myst2204@@'  // 이 부분을 변경
};
```

### 2. JSON 설정 파일 참조

`public/admin-config.json` 파일에 관리자 정보가 저장되어 있습니다:

```json
{
  "admin": {
    "id": "admin",
    "password": "myst2204@@",
    "name": "관리자",
    "role": "administrator"
  }
}
```

⚠️ **중요**: 이 파일은 `.gitignore`에 포함되어 있어 Git에 커밋되지 않습니다.

## 대시보드 기능

### 📊 통계 카드

#### 1. 총 사용자 수 👥
- 시스템을 사용한 고유 사용자 수
- 사용자 ID 기준으로 카운트

#### 2. 총 로그인 횟수 🔐
- 전체 로그인 세션 수
- 같은 사용자가 여러 번 로그인한 경우 모두 카운트

#### 3. 총 사용 시간 ⏱️
- 모든 사용자의 누적 사용 시간
- 시간/분 단위로 표시

#### 4. 평균 세션 시간 📈
- 세션당 평균 사용 시간
- 총 사용 시간 ÷ 총 로그인 횟수

### 🎛️ 필터 및 컨트롤

#### 사용자 필터
```
전체 사용자  - 모든 사용자 데이터
fintech01    - 핀테크팀 사용자만
gh01         - 개발팀 사용자만
mg01         - 마케팅팀 사용자만
samsung01    - 영업팀 사용자만
```

#### 통계 유형
- **일별**: 날짜별 통계 (YYYY-MM-DD)
- **주별**: 주 단위 통계 (월요일 시작)
- **월별**: 월 단위 통계 (YYYY-MM)

#### 데이터 관리
- **📥 데이터 내보내기**: JSON 파일로 다운로드
- **🗑️ 데이터 삭제**: 모든 로그 기록 삭제 (복구 불가)

### 📈 차트

#### 로그인 횟수 차트 (막대 그래프)
- X축: 날짜/주/월
- Y축: 로그인 횟수
- 파란색 막대로 표시

#### 사용 시간 차트 (라인 그래프)
- X축: 날짜/주/월
- Y축: 사용 시간 (분)
- 보라색 선으로 표시

### 📋 상세 로그 테이블

| 컬럼 | 설명 |
|------|------|
| 사용자 ID | 로그인한 사용자의 ID |
| 날짜 | 로그인 날짜 (YYYY-MM-DD) |
| 로그인 시간 | 로그인한 시각 |
| 로그아웃 시간 | 로그아웃한 시각 (진행 중이면 "진행중") |
| 사용 시간 | 로그인부터 로그아웃까지의 시간 |

## 사용자별 분석

### fintech01 사용자 보기
1. 사용자 필터에서 "fintech01" 선택
2. 해당 사용자의 모든 통계가 필터링되어 표시
3. 차트와 테이블이 자동으로 업데이트

### 기간별 분석
1. **일별 분석**: 매일의 사용 패턴 파악
2. **주별 분석**: 주간 사용 트렌드 확인
3. **월별 분석**: 장기 트렌드 분석

## 데이터 백업

### JSON 파일로 내보내기

1. "📥 데이터 내보내기" 버튼 클릭
2. 파일명: `medalo_usage_logs_YYYY-MM-DD.json`
3. 다운로드 위치: 브라우저 기본 다운로드 폴더

### 백업 파일 구조

```json
[
  {
    "userId": "fintech01",
    "loginTime": "2025-11-15T10:30:00.000Z",
    "logoutTime": "2025-11-15T12:45:00.000Z",
    "duration": 8100000,
    "date": "2025-11-15"
  }
]
```

### 권장 백업 주기
- **일일 백업**: 중요한 프로젝트
- **주간 백업**: 일반적인 사용
- **월간 백업**: 최소 권장 사항

## 보안 가이드

### 🔒 로그인 보안
1. **세션 기반 인증**
   - 브라우저 탭 닫으면 자동 로그아웃
   - 새로고침 시에는 세션 유지

2. **비밀번호 보안**
   - 최소 8자 이상
   - 특수문자 포함
   - 정기적으로 변경

### 📁 데이터 보안
1. **로컬스토리지 사용**
   - 서버 없이 브라우저에 저장
   - HTTPS 사용 권장

2. **백업 관리**
   - 정기적으로 백업
   - 안전한 장소에 보관
   - 암호화 권장

### 🚨 주의사항
- ❌ 공용 컴퓨터에서 사용 금지
- ❌ 로그인 정보 공유 금지
- ❌ 백업 파일을 공개 저장소에 업로드 금지
- ✅ 사용 후 반드시 로그아웃
- ✅ HTTPS 사용
- ✅ 정기적인 비밀번호 변경

## 트러블슈팅

### 로그인이 안 돼요
**확인 사항**:
1. ID: `admin` (소문자)
2. 비밀번호: `myst2204@@` (정확히 입력)
3. Caps Lock 확인
4. 특수문자 입력 확인

### 데이터가 안 보여요
**해결 방법**:
1. 사용자가 실제로 로그인했는지 확인
2. 브라우저 개발자 도구 → Application → Local Storage 확인
3. `medalo_usage_logs` 키 확인
4. 페이지 새로고침

### 차트가 표시되지 않아요
**해결 방법**:
1. 인터넷 연결 확인 (Chart.js CDN)
2. 브라우저 콘솔에서 에러 확인
3. 페이지 새로고침
4. 다른 브라우저에서 시도

### 로그아웃이 안 돼요
**해결 방법**:
1. 브라우저 캐시 삭제
2. 시크릿/프라이빗 모드에서 접속
3. 브라우저 개발자 도구에서 세션 스토리지 삭제:
   ```javascript
   sessionStorage.removeItem('dashboard_auth');
   ```

## 고급 기능

### 사용자 추가

`public/admin-config.json`에 사용자 추가:

```json
{
  "allowedUsers": [
    {
      "id": "newuser01",
      "name": "신규 사용자",
      "department": "부서명"
    }
  ]
}
```

### 커스텀 리포트

브라우저 콘솔에서 실행:

```javascript
// 특정 사용자의 총 사용 시간
const logs = JSON.parse(localStorage.getItem('medalo_usage_logs'));
const userLogs = logs.filter(log => log.userId === 'fintech01');
const totalTime = userLogs.reduce((sum, log) => sum + log.duration, 0);
console.log('총 사용 시간:', Math.floor(totalTime / 60000), '분');

// 가장 활발한 요일 찾기
const dayCount = {};
logs.forEach(log => {
  const day = new Date(log.date).toLocaleDateString('ko-KR', { weekday: 'long' });
  dayCount[day] = (dayCount[day] || 0) + 1;
});
console.log('요일별 로그인:', dayCount);
```

## API 참조

대시보드는 다음 함수들을 사용합니다:

```javascript
// 데이터 조회
getAllLogs()           // 모든 로그
getLogsByUser(userId)  // 특정 사용자 로그

// 통계
getDailyStats()        // 일별 통계
getWeeklyStats()       // 주별 통계  
getMonthlyStats()      // 월별 통계

// 데이터 관리
exportLogsAsJson()     // JSON 내보내기
clearAllLogs()         // 모든 데이터 삭제
```

## 문의

문제가 발생하거나 도움이 필요하면 개발팀에 문의하세요.

---

**Made by MedAlo** 🔐

